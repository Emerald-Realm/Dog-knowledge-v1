import React from 'react'

function Biglist(props) {
    return (
        <div>
            <ul className="big">
                <li >{props.cede}</li>
            </ul>
        </div>
    )
}

export default Biglist
